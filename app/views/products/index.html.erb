<h1>Les produits du congel</h1>

<div class="form_add">
  <%= render 'shared/form_add' %>
</div>
<div>******************************************</div>
<ul>
<% @products.each do |product| %>
<li>
  <%= product.quantity %> <%= product.name %>  depuis le <%= product.entered_on %>
  <%= link_to 'X', product_path(product), method: :delete %>
</li>
<% end %>
</ul>
